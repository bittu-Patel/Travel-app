import Image from "next/image"

type:
title:
icon?:
variant

const Button = ({ type, title, icon, variant }: ButtonProps) => {
  return (
<button 
type='button'
>
Login
</button> 
)
}

export default Button